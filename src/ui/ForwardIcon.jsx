import styled from "styled-components";

const Icon = styled.span`
  margin-left: 0.75rem;
  font-size: 2rem;
`;

function ForwardIcon() {
  return <Icon>&rsaquo;</Icon>;
}

export default ForwardIcon;
